'use client'
import {createTheme} from "@mui/material";

const theme = createTheme({
    palette : {
        background : {
            paper : '#f1f1f1',
            default : '#fff',
        },
    },
});

export default theme;